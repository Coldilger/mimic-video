model/checkpoints
eval/*/results
nohup.out
uv.lock
.vscode/
wandb/
__pycache__/
.venv/
# Experiment artifacts: extracted features and SLURM job logs. Only code and
# the written-up results (README.md) belong in the repo -- matching what
# experiment1_ablation/ commits. The .npz feature files are ~6MB each and are
# regenerable from extract.slurm.
eval/*/experiment*/*.npz
eval/*/experiment*/*.out
